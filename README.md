# Android port of [Radamsa](https://gitlab.com/akihe/radamsa)

Just a generated code export with minor Android NDK tweaking to compile and support Android ABIs.
Run `ndk-build` from root directory to compile
